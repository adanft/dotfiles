# VM profile: lock only. Avoid DPMS and suspend because virtual displays and
# hypervisor power handling vary a lot and can leave sessions hard to recover.

general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
}

listener {
    timeout = 900
    on-timeout = loginctl lock-session
}
